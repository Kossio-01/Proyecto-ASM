{
  "name": "proyecto-asm-root",
  "version": "1.0.0",
  "description": "Proyecto ASM Pance - Root package",
  "scripts": {
    "start": "cd backend && node server.js",
    "build": "cd backend && npm install",
    "dev": "cd backend && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0"
  }
}