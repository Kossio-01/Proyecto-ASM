{
  "name": "asmpance-api",
  "version": "1.0.0",
  "description": "API simple para marcadores del Río Pance",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "cd public/Front/ASM/proyecto1 && npm install && npm run build",
    "build:react": "cd public/Front/ASM/proyecto1 && npm run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}